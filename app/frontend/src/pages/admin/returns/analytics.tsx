import React from 'react';
import { ReturnAnalyticsDashboard } from '../../../components/Admin/returns/ReturnAnalyticsDashboard';

/**
 * Return Analytics Page
 * 
 * Displays comprehensive analytics on return patterns, trends, and performance metrics
 * including:
 * - Trend visualization showing weekly return patterns
 * - Category breakdown charts (pie and bar charts)
 * - Seller performance tables with key metrics
 */
const ReturnAnalyticsPage: React.FC = () => {
  return <ReturnAnalyticsDashboard />;
};

export default ReturnAnalyticsPage;
