<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Seller Invoice <%= invoiceNumber %></title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    html, body { width: 100%; font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif; color: #2c3e50; background: white; }
    body { padding: 40px; line-height: 1.5; }
    .container { max-width: 800px; margin: 0 auto; }
    .section { margin-bottom: 30px; }
    .section-title { font-size: 12px; font-weight: 700; color: #8e44ad; text-transform: uppercase; margin-bottom: 10px; border-bottom: 1px solid #eee; padding-bottom: 5px; }
    .party-info { display: flex; gap: 40px; margin-bottom: 30px; }
    .party-block { flex: 1; font-size: 12px; }
    .party-block strong { color: #2c3e50; display: block; margin-bottom: 5px; font-size: 13px; }
    .party-block p { color: #7f8c8d; margin: 2px 0; }
    .items-table { width: 100%; border-collapse: collapse; margin-bottom: 20px; font-size: 12px; }
    .items-table th { background: #f9f9f9; padding: 10px; text-align: left; font-weight: 700; border-bottom: 2px solid #8e44ad; }
    .items-table td { padding: 10px; border-bottom: 1px solid #eee; }
    .text-right { text-align: right; }
    .text-center { text-align: center; }
    .summary-section { display: flex; justify-content: flex-end; }
    .summary-table { width: 300px; font-size: 12px; }
    .summary-row { display: flex; justify-content: space-between; padding: 8px 0; border-bottom: 1px solid #eee; }
    .summary-row.total { border-top: 2px solid #8e44ad; font-weight: 700; font-size: 14px; color: #8e44ad; margin-top: 5px; }
    .notes-section { font-size: 11px; color: #7f8c8d; background: #f9f9f9; padding: 15px; border-radius: 5px; }
  </style>
</head>
<body>
  <div class="container">
    <%- include('../partials/pdf-header', { 
      title: 'Seller Invoice', 
      idLabel: 'Invoice', 
      idValue: invoiceNumber, 
      date: issueDate, 
      status: status || 'paid' 
    }) %>

    <div class="party-info">
      <div class="party-block">
        <p class="section-title">Seller</p>
        <strong><%= sellerName || 'Seller Reference' %></strong>
        <p>Wallet: <%= sellerId %></p>
      </div>
      <div class="party-block">
        <p class="section-title">Billing From</p>
        <strong>LinkDAO Infrastructure</strong>
        <p>Commission Services</p>
      </div>
    </div>

    <div class="section">
      <table class="items-table">
        <thead>
          <tr>
            <th>Description</th>
            <th class="text-center">Count</th>
            <th class="text-right">Base Amount</th>
            <th class="text-right">Commission</th>
          </tr>
        </thead>
        <tbody>
          <% items.forEach(item => {
            if (item) { %>
              <tr>
                <td><%= item.description %></td>
                <td class="text-center"><%= item.quantity || 1 %></td>
                <td class="text-right"><%= currency %> <%= (item.amount || 0).toFixed(2) %></td>
                <td class="text-right"><%= currency %> <%= (item.commission || 0).toFixed(2) %></td>
              </tr>
            <% }
          }); %>
        </tbody>
      </table>
    </div>

    <div class="section summary-section">
      <div class="summary-table">
        <div class="summary-row">
          <span>Gross Sales</span>
          <span><%= currency %> <%= (subtotal || 0).toFixed(2) %></span>
        </div>
        <div class="summary-row">
          <span>Platform Fees</span>
          <span><%= currency %> <%= (taxAmount || 0).toFixed(2) %></span>
        </div>
        <div class="summary-row total">
          <span>NET PAYOUT</span>
          <span><%= currency %> <%= (totalAmount || 0).toFixed(2) %></span>
        </div>
      </div>
    </div>

    <%- include('../partials/pdf-footer') %>
  </div>
</body>
</html>
