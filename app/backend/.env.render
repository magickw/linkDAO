# Render Environment Configuration
# Copy these variables to your Render service environment variables

NODE_ENV=production
PORT=10000

# Frontend URL (update with your actual frontend domain)
FRONTEND_URL=https://linkdao.io
CORS_ORIGIN=https://linkdao.io,https://www.linkdao.io,https://linkdao-git-main.vercel.app,https://linkdao-frontend.vercel.app

# Optional: Database and Redis (if you add them later)
# DATABASE_URL=postgresql://user:password@host:port/database
# REDIS_URL=redis://host:port

# Optional: External API keys (add as needed)
# OPENAI_API_KEY=your_openai_key
# STRIPE_SECRET_KEY=your_stripe_key

# Memory optimization
# IMPORTANT: Adjust based on your Render plan
# - Starter (512MB RAM): Use --max-old-space-size=384 (leave headroom for system)
# - Standard (2GB RAM): Use --max-old-space-size=1536 (recommended for production)
# - Pro (4GB+ RAM): Use --max-old-space-size=3072 or higher
# Note: Setting too high will cause OOM crashes. Setting too low causes frequent GC and alerts.
NODE_OPTIONS=--max-old-space-size=1536 --expose-gc